/**
 * Composants Aceternity UI (copy-paste depuis https://ui.aceternity.com)
 *
 * Ajouter un composant :
 *   1. Copier le code depuis la doc Aceternity
 *   2. Le placer ici (ex. `spotlight.tsx`)
 *   3. L'exporter ci-dessous
 *
 * Dépendances déjà installées : framer-motion, lucide-react, @tabler/icons-react,
 * mini-svg-data-uri, clsx, tailwind-merge
 */

export {};
